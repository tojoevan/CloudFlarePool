# 03 · 待办（决议 / 待讨论 / 实施）

> 状态：评审中 ｜ 最后更新：2026-08-15 ｜ 负责人：微家事开发组
> 关联文档：`02-设计.md`（方案）· `01-需求.md`（需求）· `04-检视.md`（风险与验证）· `05-开发指南.md`（落地节奏）

## 一、已决议（Architecture Decision Log）

| # | 日期 | 决议 | 说明 |
|---|---|---|---|
| D1 | 2026-08-15 | 数据湖 = **app-agnostic 多应用平台底座** | 不是微家事专属后台；新应用靠"声明 schema"接入，不改数据湖代码 |
| D2 | 2026-08-15 | 抽象层级 = **平台核心 → 应用(app) → 租户(tenant) → 用户(user 体系)** | 见 `02-设计.md` §2 |
| D3 | 2026-08-15 | 隔离模型 = **`app_id` + `tenant_id` 两级** | 单库共用、逻辑隔离；R2 前缀 `app_id/tenant_id/` |
| D4 | 2026-08-15 | JWT 算法 = **EdDSA/Ed25519**；JWT 与 X-Sync-Key **物理分离** | 见 `02-设计.md` §5 |
| D5 | 2026-08-15 | 核心通用度 = **(a) 类型化应用模块** | app 声明 collection schema，数据湖提供类型感知 CRUD + 查询 |
| D6 | 2026-08-15 | 端点形态 | 客户端 URL 保持 `/v1/t/:tenant/...`，`app` 由 JWT `aid` + tenant 反查；平台/应用管理用 `/v1/a/:app/...` |
| D7 | 2026-08-15 | 管理后台 = 第一等 Web 客户端 | UI 托管网关（境内、快、合规），走平台 API（admin scope） |
| D8 | 2026-08-15 | JWT 公共 claims 含 `aid` | `sub` / `aid` / `tid` / `typ` / `scp` / `exp` / `iss` / `aud` |
| D9 | 2026-08-15 | 管理端 = **单一平台级管理端应用 + 按 scope 分层（平台/应用/租户）** | 不做「每应用独立后台」；独立运维靠 scope 隔离实现；类型化模块动态渲染；第三方可自建客户端。对应 `02-设计.md` §11 |
| D10 | 2026-08-15 | **实施排序：B1（双模鉴权）先行、B2（账号体系）后行** | B2 实质依赖 B1（否则签出 JWT 数据湖不认 / 需全代理违背设计）；B1 不动小程序、无 DB 变更、纯向后兼容。对应 `02-设计.md` §14 |
| D11 | 2026-08-15 | 产品命名：**家事本 → 微家事**（代码 slug `jiashiben` 不变） | 文档/产品名统一改微家事；slug 留待后续任务，当前不碰代码（避免影响已发布小程序） |
| D12 | 2026-08-15 | **Q6 收口：微家事 = 单 tenant** | 消费级单一运营方；`tenant_id` 仅为平台通用性预留，当前不真正多值；家庭 = tenant 内分组/关系实体，非租户 |
| D13 | 2026-08-15 | **Q1 收口：app 开通初期 admin 创建** | 不开放自助注册（单一运营者阶段，避免审核/滥用面） |
| D14 | 2026-08-15 | **Q3 收口：小程序永远走网关代理** | 网关代持 JWT 调数据湖；小程序前端零改动；与 D10/B1 一致 |
| D15 | 2026-08-15 | **Q4 收口：管理后台首版只读** | 仅租户/数据量/健康概览；降低 admin 鉴权与审计强度 |
| D16 | 2026-08-15 | **Q5 收口：scope 粒度暂定** | 资源族×动作（`todo/task/...`×`read/write/admin`）待首个 app 接入时复核 |
| D17 | 2026-08-15 | **Q7 收口：配额/计费暂不启用** | `apps`/`tenants` 表预留 `plan`/`quota` 字段，当前免费额度充足 |
| D18 | 2026-08-15 | **Q8 收口：类型化模块形态 (a)** | schema 元数据存 `apps` 表；数据走通用表 + `app_id`+`tenant_id`+collection 名分片；新 app「声明即用」 |
| D19 | 2026-08-15 | **Q2 退役**：家庭分组 = 既有机制 | 非开放架构项；`family_groups`+邀请流程已存在于微家事代码，跨端复用，无需新设计 |

## 二、待讨论（Open Questions）

> 2026-08-15 收口：Q1–Q8 全部决议，见决策日志 **D11–D19**（含 Q2 退役）。无遗留开放架构问题。

| 原 # | 收口结论 | 决策 |
|---|---|---|
| Q1 | app 开通初期 admin 创建 | D13 |
| Q2 | 退役：家庭分组 = 既有机制，非架构项 | D19 |
| Q3 | 小程序永远走网关代理 | D14 |
| Q4 | 管理后台首版只读 | D15 |
| Q5 | scope 粒度暂定，首 app 复核 | D16 |
| Q6 | 微家事单 tenant，tenant 仅预留 | D12 |
| Q7 | 配额/计费暂不启用 | D17 |
| Q8 | 类型化模块形态 (a) | D18 |

## 三、实施阶段与任务清单

### Phase 0 — 地基文档 `[x]`
- [x] `00-总览.md` / `01-需求.md` / `02-设计.md` / `03-待办.md` / `04-检视.md` / `05-开发指南.md` / `06-变更日志.md`（7 文档体系）

### Phase 1 — 数据湖双模鉴权 + app 维度 `[x] 代码完成·测试绿·数据湖+D1 已部署(2026-08-15)；网关 signT1 签 appId 改动推迟至 Phase2 启动`（B1 先行：双模鉴权不动小程序、纯向后兼容）

**B1 核心（双模鉴权，本次先行）** — 代码已实现并本地验证通过（smoke 测试 14/14 全绿、Ed25519 round-trip 已验证）
- [x] 数据湖 `src/index.js`：纯 `X-Sync-Key` 守卫 → 双模（`Authorization: Bearer` 走 JWT 验签 + exp + scope + `aid`/`tid` 隔离；无 Bearer 回落 `X-Sync-Key` 内部通道）；小程序路径不变
- [x] 网关 `server.js` 新增 JWT 签名工具（EdDSA 私钥，`JWT_PRIVATE_KEY` env），`/api/login` 后顺带签 T1 JWT（小程序暂不用，留给直连客户端）
- [x] 生成 Ed25519 密钥对（公钥 `{"gw1":"<raw 32B base64url>"}` 待 `wrangler secret put JWT_PUBLIC_KEYS`；私钥单行 `\n` 转义 PEM 待写入网关 `.env`）
- [x] 回归验证：小程序经网关代理（`X-Sync-Key`）路径零改动、零影响（dualGuard 回落分支字节级等价）

**app 维度基础（2026-08-15 代码落地，14/14 测试绿）**
- [x] 新增 `apps` 表（app_id / name / owner / auth_methods / plan / quota / status）+ `collections` / `collections_meta` 通用表（`schema.js` / `schema.sql`）
- [x] `tenants` 表加 `app_id` 列；`migrate()` 幂等加列 + 回填 `DEFAULT_APP_ID=jiashiben`；远端 `scripts/migrate-app.sql` 提供一次性 ALTER+回填
- [x] 两维隔离：`auth.js` 新增 `resolveApp(tenant)`，`dualGuard` JWT 分支仅当 token 显式带 `aid` 且不匹配才拒（老 token 无 `aid` 不拦，0.1.5 小程序零影响）
- [x] 端点加 `/v1/a/:app/...`：建/查 app、app 维度租户 CRUD、collection schema 登记（`src/routes/apps.js`）
- [x] **类型化通用存储**：新增 `/t/:tenant/c/:collection[/:id]` 通用 CRUD（app_id 由 `resolveApp` 派生、owner 隔离）；不替掉既有硬编码 todos/tasks，纯 additive（`src/routes/data.js`）
- [x] 网关 `signT1` 签入 `appId`（默认回退 `tenantId`，未配 `APP_ID` 时行为不变）；`server.js` 新增 `CFG.appId`（默认 `TENANT_ID`）
- [x] 微家事作为首个 app：现有 `jiashiben` 租户回填 `app_id=jiashiben`，数据归属已就绪；类型化模块「声明即用」能力已具备，迁移为声明式、零数据搬运

### Phase 2 — 网关签发 JWT + 账号体系 `[x]`

> **实施设计已定稿**：`02-设计.md` §18（核心约束 / 双路径 tenant 隔离 / T2+T4+T3 增量设计 / 部署与门禁）。
> **代码落地完成（2026-08-15，本地 19/19 测试绿），尚未部署**：遵循用户"写代码不部署、不重启网关、不动小程序前端"的约束，本次仅提交代码 + 文档，**不执行 §18.7 部署步骤、不重启网关、不碰小程序**。实施中对 §18.3 的两处设计做了有意修正（bcrypt→HMAC-SHA256 / PBKDF2、账号验证归属数据湖），详见 `02-设计.md` §18.9，外部契约不变。
> **网关部署时点（采纳 A 方案）**：`signT1` 签 `appId` 与 T2/T4 登录一并部署，网关经宝塔重启一次；当前未部署。

- [x] 网关双路径 tenant 解析：X-Sync-Key 分支原样、JWT 分支按 token 的 `aid`/`tid`（`src/lib/auth.js` dualGuard 先 decode 分派）
- [x] 账号 / 密码登录端点 → 签 T2（`POST /api/account/login`，网关验 lake `/internal/account/verify` 后签）
- [x] admin 登录端点 `POST /api/admin/login` → 签 T4（网关验 lake `/internal/admin/verify` 后签）
- [x] `api_keys` 表（T3 服务令牌）+ 签发 / 轮转 / 吊销（`src/routes/keys.js` + `src/lib/auth.js` verifyServiceToken，HMAC-SHA256）
- [x] 账号验证归属数据湖：`users`/`admin_accounts` + 密码不离 lake，网关经 X-Sync-Key 转发验身份后签 T2/T4（`src/routes/account.js`）
- [x] 密钥已注入（B1）：`JWT_PRIVATE_KEY`（网关）签 T2/T4、`JWT_PUBLIC_KEYS`（数据湖）验签，无需再动
- [x] **数据湖部署完成（2026-08-15 22:34，已实际执行 A1–A4）**：`wrangler d1 execute` 建 4 表（D1 表数 7→11，幂等零破坏）+ `wrangler deploy`（生产版本 `88d02a6c-bb1e-4cc6-b4a9-5b951df7ac2c`，D1+R2 绑定正常）；验证 `/health`=200、`/t/jiashiben/todos` 无 key=403（X-Sync-Key 守卫完好）、`/internal/account/verify` 无 key=403（新路由挂载）；**小程序 X-Sync-Key 路径不受影响二次确认**；回滚极简（`wrangler rollback cloudflarepool` 回 Phase 1 版本）
- [x] **A5 网关代码同步完成（2026-08-15 22:38）**：`scp` 同步 Phase 2 改动 `server.js`+`lib/jwt.js` 到 `/www/wwwroot/home.inkspcl.com/`（**排除 `.env`**，保留生产密钥）；服务端校验 `signT2/signT4` + 两个新登录路由已就位；运行进程仍跑旧代码、未受影响
- [x] **A6 网关重启完成（用户于宝塔面板点「重启」，22:40）**：重启后 T2/T4 新路由生效；小程序路径因旧反代零改动，重启前后行为一致（仅秒级中断窗口）。运行进程 `pid 562757` 启动于 22:40:07，晚于 A5 文件落盘 22:38:19 → 确认加载的是最新代码
- [x] **A7 小程序代理回归冒烟通过（强制门禁已达成）**：服务器用真实 `SESSION_SECRET` 铸造 session（tenantId=`weijiashi`）→ `curl /api/data/todos` 经网关 X-Sync-Key 反代到 lake `/t/weijiashi/todos` → `[]` HTTP **200**。X-Sync-Key 链路 + 租户解析端到端打通；外部冒烟（`/api/health`=200、新路由 400/501/400、`/api/data` 无 token=401）全绿。**Phase 2 全链路已上线，小程序 X-Sync-Key 路径零影响**

### Phase 3 — 多端客户端 + 管理后台 `[ ]`
- [ ] Web SPA 接入（T2）
- [ ] App（原生）接入（T2）
- [ ] MCP Server 骨架（T3，含工具↔scope 映射）
- [ ] Skill（专家）复用 MCP（零额外后端）
- [ ] 管理后台 UI：平台级（`/v1/apps`）+ 应用级（`/v1/a/:app/tenants`），首版建议只读（见 Q4）

## 四、代码现状 vs 计划（Gap）

| 项 | 现状 | 目标 |
|---|---|---|
| 数据湖守卫 | 双模 + app 维度 `aid`/`tid` 校验（代码已落地、14/14 测试绿、待部署） | 双模 + app 维度 `aid`/`tid` 强制校验 ✅ |
| 隔离 | `app_id` + `tenant_id` 两级（代码已落地） | `app_id` + `tenant_id` 两级 ✅ |
| 资源 | 硬编码 todos/tasks/archive/... + 通用 `collections` CRUD（additive） | app 声明 collection，通用 CRUD ✅ |
| 网关 | `TENANT_ID` 静态 + `APP_ID` 可配（默认回退 `TENANT_ID`） | `APP_ID` + 按请求解析 tenant（Phase 2 完成按请求解析） |
| 登录 | 仅微信 code2session | + 账号/OAuth(T2) + admin(T4) + api_keys(T3) |
| 可视化运维 | 无 | 管理后台（平台级 + 应用级） |

## 五、前端 / 小程序侧待办（独立于后端 Phase，待排期）

> 说明：小程序 0.1.5 审核中（2026-08-15 上传，预计 1–7 天）。审核期按 `05-开发指南.md` §6 窗口期规则收紧门禁——前端业务代码改动不急于上线、不单独传版，统一随 0.1.6 一起走 Gate0 + G2(`lint --strict`) + 真机核心页目检。以下条目在审核期可「准备 / 写测试」但代码改动须等过审。

### 0.1.6 设计系统清理（提练自设计审计画布 Footer 6 条，待过审落地）
- [ ] `app.wxss` 补 `--space-1..6` + `--page-pad` + `--text-xs..xxl` 共 13 个 token
- [ ] 修 `family.wxss` 两处 bug：① `.invite-banner` 引用未定义 `--brand-border`（走 fallback 渲染正常）② `.member-row .avatar.family` fallback 误写 `#7BA7BC`（应为 `#7A8471`）
- [ ] 收敛魔数（40rpx→`--page-pad`、28rpx→`--text-base` 等）
- [ ] 统一 `.empty` 全局类
- [ ] 补 disabled / loading 态
- [ ] `lint --strict` + regression 复跑

### 前端测试增强
- [x] `regression-check.mjs` 新增「家庭页 family」断言段（commit `21b0d1b` 已推 `weijiashi`）：固化 design 审计两处潜在问题——`.invite-banner` 不得引用 app.wxss 未定义的 `--brand-border`、`.avatar.family` fallback 不得为错误色 `#7BA7BC`；补 `lint --strict` 仅拦「定位类同名冲突」的盲区。**现状：断言已写、当前对 0.1.5 报红 2 项（即这两处已知债务），须 0.1.6 修 family.wxss 后转绿** [2026-08-15]

### 设计系统组件库（审计结论的落地交付，支撑 0.1.6）
- [x] **组件库画布（2026-08-15 新增）**：Ardot 画布 `微家事·设计系统组件库`（fileId `715167828613451`）把设计审计结论落成可复用组件——① Token 看板（14 色板 + 5 档圆角 R1–R5 + 6 档间距 S1–S6 + 6 级字号 xs–xxl）；② 组件画廊（按钮/按钮状态 default·hover·disabled·loading / 输入框 default·focus·error·带图标 / 标签 待办·进行中·已完成·家庭成员 / 卡片 todo·task·archive / 头像 member·family·guest / 底部操作栏 .action-bar 列表胶囊 + .edit-actions 全宽双按钮）；③ 状态（空 / 加载）；④ 页面组合预览（今日 / 家庭两屏，家庭屏含 invite-banner 与成员头像组，验证 family.wxss 两处修复后的正确视觉）。导出 PNG/PDF 存 `docs/微家事-设计系统组件库.{png,pdf}`，直接作为 0.1.6 样式清理 6 条与 family.wxss 两处修复的实现参考。源画布保留，可微调后重导出覆盖。

## 六、变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-15 | 由 `PLATFORM_TODO` 重组为待办文档，纳入 7 文档体系 |
| 2026-08-15 | 新增决策 D9（单一多 scope 分层管理端）；对应 `02-设计.md` §11 升级 |
| 2026-08-15 | 新增决策 D10（B1 双模鉴权先行 / B2 账号体系后行，含依赖分析）；Phase 1 补 B1 核心子任务（网关签 JWT / 密钥注入 / 小程序零影响回归）；对应 `02-设计.md` §14 |
| 2026-08-15 | 整轮收口：产品改名微家事（D11）、Q6 单租户（D12）、Q1/Q3/Q4/Q5/Q7/Q8 收口（D13/原D11–D18）、Q2 退役（D19）；`02-设计.md` 加 §15 现状基线 + §16 Phase1 B1 实施设计；全局改名 家事本→微家事 |
| 2026-08-15 | 新增「五、前端 / 小程序侧待办（待排期）」：并入 0.1.6 设计系统清理 6 条 + 前端回归测试增强 1 条（family.wxss 断言）；对应审核窗口期规则，前端改动统一随 0.1.6 走门禁 |
| 2026-08-15 | **Phase 1 app 维度基础代码落地并生产部署（纯 additive，14/14 smoke 测试绿）**：`apps`/`collections`/`collections_meta` 新表 + `tenants.app_id` 幂等迁移；`resolveApp` 两维隔离；`/v1/a/:app` 管理端点；通用 `collections` CRUD；`wrangler deploy` 数据湖 + 远端 `scripts/migrate-app.sql` 已执行（版本 `078eb858...`，微家事注册为首 app）。网关 `signT1` 签 `appId` 改动**已写好未部署**，按 A 方案推迟至 Phase 2 启动部署 |
| 2026-08-15 | **网关部署时点拍板（采纳 A 方案）**：Phase 1 的 `signT1` 签 `appId` 改动不在审核期单独部署，推迟至 Phase 2 启动、与 T2/T4 登录（首个消费 JWT 客户端）一起经宝塔重启部署。理由：小程序走 `X-Sync-Key`（`D14`）不消费 JWT，该改动当前无消费者、无紧迫性；后端部署不受小程序审核阻塞（§6.0）但属生产网关重启，不抢跑 |
| 2026-08-15 | **设计系统组件库画布交付**：新建 Ardot 画布 `微家事·设计系统组件库`（fileId `715167828613451`），把设计审计结论落成可复用组件（Token 看板 + 组件画廊 + 状态 + 今日/家庭页面预览），导出 PNG/PDF 存 `docs/`，作为 0.1.6 样式清理 6 条与 family.wxss 两处修复的实现参考（审核窗口期不受阻塞）|
| 2026-08-15 | **Phase 2 实施设计定稿**：`02-设计.md` 新增 §18（B2 账号体系），明确"小程序前端零改动 + 代理路径 tenant 来源不变"硬约束、网关双路径 tenant 解析隔离设计、T2/T4/T3 数据模型与端点增量、网关重启后小程序代理回归冒烟门禁项；`03-待办` Phase 2 章节标注实施设计已定稿 |
