# 04 · 检视（安全合规 / 发布前清单 / 验证记录）

> 状态：评审中 ｜ 最后更新：2026-08-16 ｜ 负责人：微家事开发组
> 关联文档：`02-设计.md`（安全设计）· `05-开发指南.md`（发布节奏）· `06-变更日志.md`（版本基线）· `07-测试.md`（测试方案/用例/记录）

## 1. 目的

集中承载"核验类"资产：安全/合规审计清单、发布前检查清单、风险登记。测试方案、用例与执行记录已独立归档至 `07-测试.md`，本文档不再重复承载，仅保留安全/合规/发布/风险。让每次检视有统一入口，避免清单散落。

## 2. 安全与合规审计清单

### 2.1 JWT 安全清单（`02-设计.md` §5）
- [ ] 私钥仅存 env / secret，不入仓库、不打日志
- [ ] 数据湖只持公钥
- [ ] JWT 与 X-Sync-Key 物理分离
- [ ] 所有令牌带 `exp` + `aud` + `kid`
- [ ] 租户隔离服务端强制校验
- [ ] 预留轮换机制（并行 kid + 宽限期）

### 2.2 开源脱敏核查（已执行）
- [x] 扫描真实域名 / AppID（`wx8e79e7c073ff5737`、`home.inkspcl.com`、`kapibala.icu`）→ 全部以占位符替换
- [x] 含真实 AppID 的 `project.config.json` / `project.private.config.json`、`.env`、`node_modules` 均 gitignore 排除
- [x] 提交后 `git grep` 兜底核查：已提交文件树无任何真实域名/AppID 残留
- [x] 数据湖 `.dev.vars`（含 `INTERNAL_KEY` 等密钥）被 gitignore，未被跟踪
- 模板要求：`*.example` / README 占位片段一律不写真实域名/IP/凭据（见 `05-开发指南.md`）

### 2.3 ICP 备案合规约束（架构硬约束）
- [ ] 备案域名须解析至**境内合规服务器**，严禁解析到境外/港澳台节点（否则注销备案 → 小程序合法域名失效）
- [ ] 备案主体须与小程序主体一致
- [ ] 备案期间不动主体 / 域名 / 小程序名称
- [ ] 数据湖域未备案 → 仅服务间/集成/海外客户端；国内终端用户走网关

## 3. 发布前检查清单（小程序，来自实战踩坑）

- [ ] **开发者工具整退重开**再编译（新增页面后 `lazyCodeLoading` 会报 `search.wxml not found`，菜单清缓存救不了，须 `⌘Q` 退出重开）
- [x] **真机 / 体验版自测关键路径**（2026-08-16 实测）：搜索 / 新建事务·待办 / 保存关页 / 修改 全部通过；家庭成员邀请因「小程序认证前无法发送」被外部前置约束阻断（非代码缺陷，不阻塞 0.1.6）
- [ ] **提前备好《隐私政策》《用户协议》PDF**（发布需同步上传，个人主体也要）
- [ ] `__subPageFrameEndTime__ of null` 属框架无害噪音，别为它卡发布

### 3.1 已知限制（2026-08-16 实测）
- **家庭成员邀请需小程序完成微信认证后方可发送**：未认证时邀请入口不可用，属账号层面的外部前置条件，非 0.1.6 代码缺陷，不阻塞本次发布。
- 0.1.6 候选目前挂起，重审锚点 2026-08-19；继续使用 0.1.5 稳定期，发现问题再并入 0.1.6。

> 本清单为「发布流程」的详细检查项；端到端编排与 Gate 判定见 `05-开发指南.md` §6。

## 4. 测试类资产迁移说明

> 测试方案、用例与执行记录已独立归档至 `07-测试.md`（2026-08-15 迁出）。
> 本文档聚焦：安全/合规审计、发布前检查、风险登记。

## 5. 风险登记

| # | 风险 | 缓解 |
|---|---|---|
| R1 | 备案驳回/超时 | 盯 12381 短信 24h 核验、接 0755 回访；详见 `05-开发指南.md` |
| R2 | Cloudflare 国内访问延迟 | 国内终端用户走境内网关；MCP/Skill 等海外路径可放宽 |
| R3 | 密钥泄露 | 私钥仅 env/secret；数据湖只持公钥；轮转机制 |
| R4 | 网关耦合数据模型 | 界面在网关、权威在数据湖的 hybrid 边界（见 `02-设计.md` §9/§11） |

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-15 | 新建检视文档，归入 7 文档体系；吸收发布前清单与安全/合规清单 |
| 2026-08-15 | 验证记录迁出至 `07-测试.md`；本文档收敛为安全/合规/发布/风险 |
