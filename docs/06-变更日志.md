# 06 · 变更日志

> 状态：已定稿 ｜ 最后更新：2026-08-15 ｜ 负责人：微家事开发组
> 关联文档：`05-开发指南.md`（版本进位约定）· `03-待办.md`（实施阶段）

## 1. 目的

集中记录各模块的版本基线，作为回滚与进度参照。每次重大变更在对应小节追加条目（最新在上）。

## 2. 小程序版本基线（微家事）

进位规则：`X.Y.Z` 单数字、**遇 9 进位**（如 `0.0.9 → 0.1.0`）。

| 版本 | 内容 | 状态 |
|------|------|------|
| 0.0.7 | 基准已上传 | 已发布 |
| 0.0.8 | 1s 延迟修复 | 已发布 |
| 0.0.9 | closeEdit 加固 | 已发布 |
| **0.1.0** | subPageFrame 红错修复 + 家庭成员功能 | 已发布（从 0.0.9 进位） |
| 0.1.1 | 全局搜索 | 待上传 |
| 0.1.2 | 底部操作条合并（搜索 + 新建） | 待上传 |
| 0.1.3 | 搜索框落位 / 胶囊避让等细化 | 待上传 |
| 0.1.4 | 标签自定义 + 事务新建入口修复 | 已并入 0.1.5（统一提审） |
| **0.1.5** | 累积批次统一提审（含 0.1.1–0.1.4 全部内容） | 已上传审核中（2026-08-15） |
| **0.1.6** | 修复个人/家庭空间切换不隔离 + 设计系统规范化 + 家庭页样式修复 + 回归测试补全（详见 §2.1） | 待上传 |

> 进位链：`0.0.7` → `0.0.8` → `0.0.9` → **`0.1.0`** → `0.1.1` → `0.1.2` → `0.1.3` → `0.1.4` → **`0.1.5`（统一提审）** → `0.1.6`
> 0.1.1–0.1.4 本地累积批次已并入 0.1.5 统一提审（见 `05-开发指南.md` §5.3）；0.1.6 为下一发布版本，内容见下方 §2.1。

## 2.1 0.1.6 版本说明（发布前填微信小程序后台）

- **版本号**：`0.1.6`（从 `0.1.5` 进位：patch 5→6，单数字无进位）
- **发布状态**：待上传（0.1.5 已发布，0.1.6 为下一候选；遵循 `05-开发指南.md` §6.6 窗口期门禁）
- **发布前门禁清单**（本次已全部通过）：
  - Gate0：已知缺陷冻结（无 known-broken）
  - `npm run lint --strict` 全绿（同名类扫描 21 处，高危 0 处）
  - `npm test`（regression）全绿——新增空间过滤断言后共 28 条断言通过
  - 真机核心页面目检（今天 / 事务 / 档案 / 我的 / 家庭 / 编辑 / 搜索）通过
  - 空间隔离验收：个人/家庭切换后 `today` / `archive` / `mine` 三页列表与统计正确过滤

### 微信小程序版本说明（复制下方代码块到 mp.weixin.qq.com 上传页「版本说明」）

```
微家事 0.1.6 更新内容：
· 修复个人/家庭空间切换后内容未隔离的问题，切换空间后列表与统计现在会正确过滤
· 修复家庭页成员头像底色的显示样式
· 提升各页面渲染稳定性，新增回归测试防止同类问题复发
（注：本版不引入视觉风格大改，沿用现有小程序样式；画布 B 重设计方案本轮不采用，待稳定运营后再评估）
```

> 纯文本文件（含门禁清单 + 技术变更，复制时只取上方代码块）：[`docs/微家事-0.1.6-版本说明.txt`](微家事-0.1.6-版本说明.txt)

### 技术变更清单（内部，不进版本说明）

**功能修复**
- 新增 `utils/space.js`：统一导出 `filterBySpace(todos, space)` / `inSpace(item, space)`，家庭空间判定为 `dot==='family' || shared===true`
- `pages/today/today.js`：`onShow` / `setSpace` 接入空间过滤，移除早期误加的 `computeSummary`
- `pages/archive/archive.js`：缓存 `_allItems` 全量，加载与切换空间后按空间过滤
- `pages/mine/mine.js`：todo / archive / record 计数按 `inSpace` 统计

**视觉与样式**
- `pages/today/today.wxml`：顶部结构重构为 `.home-header`，移除右上角 favicon（被小程序胶囊遮挡）
- `pages/today/today.wxss`（新建）：页面作用域 `--brand:#FF7A45` 局部换肤，新增 `.home-sub` 样式
- `app.wxss`：补全 `--brand-border` token，修复 `family.wxss` 引用未定义变量
- `pages/family/family.wxss`：`.avatar.family` fallback `#7BA7BC` → `#7A8471`
- `.sub` → `.home-sub` 重命名，避免与全局/其他页面类名冲突

**测试与门禁**
- `scripts/regression-check.mjs` 新增 13 条空间过滤功能断言
- `package.json` version `0.1.5` → `0.1.6`
- `npm run preflight`（lint + test）全绿

**网关侧配套**
- 服务器 `/www/wwwroot/home.inkspcl.com/.env` 追加 `MINIAPP_VERSION=0.1.6`
- 需宝塔面板重启网关，使版本矩阵读取到 `0.1.6`


## 3. 后端 / 数据湖

| 阶段 | 状态 | 说明 |
|------|------|------|
| 纯 X-Sync-Key 守卫 | 当前线上 | 网关持 AppID/AppSecret 做 code2session，以 X-Sync-Key 反代数据湖 |
| 双模鉴权（X-Sync-Key + Bearer JWT） | **数据湖 + 网关均已上线（2026-08-15）** | 数据湖 `dualGuard` + 网关签 T1 JWT；小程序路径不变。数据湖 `wrangler deploy`（Version `c907eca6`，公钥 secret 已注入）；网关 `home.inkspcl.com`（宝塔，`/www/wwwroot/home.inkspcl.com`）已同步 B1 代码（`server.js`/`lib/jwt.js`）+ `.env` 注入 `JWT_PRIVATE_KEY` 并重启。端到端验证：网关真实私钥签 JWT → 数据湖返回 200；`/api/health` 200；小程序走 X-Sync-Key 路径零影响。详见 `02-设计.md` §16.4、`07-测试.md` §6 |
| app_id + tenant_id 两级隔离 | **已部署（2026-08-15）** | 数据湖 `wrangler deploy`（Version `078eb858-f57a-4004-9da7-7f4a7c293241`）+ 远端 D1 跑 `scripts/migrate-app.sql`（`tenants.app_id` 加列+回填、`apps`/`collections`/`collections_meta` 建表、注册 `jiashiben` 为首个 app）；`/v1/a/:app` 生产冒烟 403（路由已挂载、守卫生效）、`/` 与 `/health` 200。纯 additive，0.1.5 小程序 X-Sync-Key 路径零影响。详见 `03-待办.md` Phase 1、`07-测试.md` §6 |
| 平台级管理后台（T4） | **数据湖 + 网关均已上线（2026-08-16）** | 数据湖 `src/routes/admin.js`（`/admin/stats`、`/admin/tenants`，`typ==='admin'` 前置校验，`countForTenant` 仅统计真实表 `todos`/`tasks_doc`/`archive_items`/`users`）+ 网关 `signT4`（`/api/admin/login`）+ `/api/t4data/*` 代理（仅放行 `typ=admin`，原样转发到 lake `/admin/*`）+ `/admin/*` 托管 `public/admin/`。验证：admin 登录 200 签 T4 → `/api/t4data/stats` 200 返回真实聚合（`todos:2`/`tasks:0`/`archives:0`/`users:1`）；T2 Bearer 打 T4 通道 403；无 token 401。数据湖 `wrangler deploy`（Version `c75e6ab0-1cf1-4c80-ad8a-a8ffdf9d6ea9`），网关同步 `server.js`+`public/admin/` 并重启。详见 `03-待办.md` Phase 3、`07-测试.md` §6 |
| T3 服务令牌消费侧 MCP Server | **已上线验证（2026-08-16）** | 新增 `mcp/`（零依赖 stdio MCP server，`server.js`+`lib/token.js`(HS256 签 T3)+`lib/datalake.js`(REST client)+`README.md`），工具 `list_todos`(data:read)/`add_task`(data:write)/`search_archive`(data:read)；签发真实 T3 key `svc_JV7ZHRdICJCG`（`tenant_bound=weijiashi`，scope `data:read`+`data:write`，raw_secret 不进仓库）。**端到端验证 list/add/search/delete 全 200、租户隔离生效**，内联 client 拉起 server.js 跑通 `initialize`/`tools/list`/`tools/call` 返回真实数据。Skill 复用经 WorkBuddy/Claude MCP connector 零额外后端。无数据湖重部署（api_keys 表 Phase 2 已建）。详见 `03-待办.md` Phase 3、`07-测试.md` §6 |

## 4. 文档变更

| 日期 | 变更 |
|------|------|
| 2026-08-15 | 地基文档 v0.2：`PLATFORM_API_AUTH_SPEC.md`(架构) + `JWT_KEY_SCHEME.md`(JWT) + `PLATFORM_TODO.md`(跟踪) |
| 2026-08-15 | **重组为 7 文档体系**：`00-总览` / `01-需求` / `02-设计` / `03-待办` / `04-检视` / `05-开发指南` / `06-变更日志`；原 3 份文档与 `发布维护备忘录.md` 退役 |
| 2026-08-15 | **新增 `07-测试.md`**：测试方案/用例/记录独立归档，体系扩为 8 文档；`04-检视` 验证记录迁出 |

## 5. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-16 | **设计决策：画布 B（小程序首页重设计稿）本轮不采用**：用户判定当前小程序样式无需变更，变化太大用户无法忍受，先稳定运营一段时间。0.1.6 定位为 bug 修复 + 稳定性发布，不引入视觉风格大改；用户侧版本说明已去掉"设计规范升级"等大改表述。画布 B 留作后续评估，不并入 0.1.6 |
| 2026-08-16 | **Phase 3 T3 MCP Server 上线验证**：新增 `mcp/`（零依赖 stdio MCP server，`server.js`+`lib/token.js`(HS256 签 T3)+`lib/datalake.js`(REST client)+`README.md`）；工具 `list_todos`(read)/`add_task`(write)/`search_archive`(read)。签发真实 T3 key `svc_JV7ZHRdICJCG`（`tenant_bound=weijiashi`，scope `data:read`+`data:write`，raw_secret 不进仓库）。**端到端门禁全绿**：内联 client 拉起 server.js→`initialize`→`tools/list`(3 工具)→`tools/call list_todos` 返回真实数据(count=2)；T3 token 直连数据湖 list/add/search/delete 全 200，租户隔离+owner=all 工作。Skill 复用经 WorkBuddy/Claude MCP connector 零额外后端。`03-待办` Phase 3 MCP/Skill 打勾、`06-变更日志` §3 补行 |
| 2026-08-16 | **Phase 3 平台级管理后台（T4）上线验证**：数据湖 `src/routes/admin.js`（`/admin/stats`、`/admin/tenants`，`typ==='admin'` 前置校验）；网关 `server.js` 新增 `/api/admin/login`（签 T4）+ `/api/t4data/*` 代理（仅放行 `typ=admin`）+ `/admin/*` 托管 `public/admin/`；vanilla JS 管理后台 SPA（登录→统计卡片）。播种 admin `tojoevan@gmail.com`(role=tenant)。**端到端门禁全绿**：admin 登录 200 签 T4→`/api/t4data/stats` 200 返回真实聚合（`todos:2`/`tasks:0`/`archives:0`/`users:1`）；T2 打 T4 通道 403、无 token 401；小程序路径零改动。数据湖 `wrangler deploy`（Version `c75e6ab0-1cf1-4c80-ad8a-a8ffdf9d6ea9`）+ 网关 scp 同步重启。访问 `https://home.inkspcl.com/admin/`。`03-待办` Phase 3 管理后台打勾、`06-变更日志` §3 补行 |
| 2026-08-16 | **安全收尾（App 暂缓）**：轮换管理员临时密码（新强口令 PBKDF2-SHA256 @100k 迭代，旧 `YswU2chddLU!5k2rs#M8` 失效）+ 禁用 `demo@weijiashi.app` 种子账号（`users.status='disabled'`）。验证：新密码 admin 登录 200 签 T4、旧密码 401、demo 401。T2/T4/小程序路径零改动、无数据湖重部署。⚠️ 排障：首版误用 200k 迭代超 Worker CPU 预算致 admin/verify 被杀死（网关 502），降回 100k 即恢复；**PBKDF2 迭代上限 100k** |
| 2026-08-16 | **T4 管理后台 — 账号与密钥中心上线**：数据湖 `src/routes/admin.js` 扩展 6 端点（`/admin/me/password` 自助改密、`/admin/users` 列表+启停、`/admin/keys` 签发/列表/`/:id/revoke` 吊销），新增 `src/lib/password.js`（PBKDF2-SHA256 @100k 边界统一）；管理后台 SPA 重做为 4 tab（概览/用户账号/服务密钥/安全，`#app` 上限 480→1080px 宽屏自适应）。数据湖 `wrangler deploy`（Version `3ced3fde`）+ 网关 scp 同步 `public/admin/*`（静态文件，免重启）。端到端全绿 |
| 2026-08-16 | **🔴 改密 NULL-hash Bug 修复**：现象——界面改密后原/新密码均 401 无法登录。根因：`/admin/me/password` 调 `hashPassword` 写库时**缺少合法性防护**，一旦计算结果异常即以 `bind(null)` 把 `admin_accounts.pwd_hash` 写成 NULL，锁死账号。修复：`admin.js` 写库前硬校验 hash 为合法 `salt$iter$hash` 字符串（否则拒绝写入并返回 400）；`smoke.test.js` 强化 `me/password` 用例（断言新密码可登录、旧密码失效、hash 非空合法）。数据湖 `wrangler deploy`（Version `17b575ff`）+ D1 写入合法 hash 恢复登录。⚠️ 教训：任何写密接口**必须先验 hash 再写库**，绝不允许写 NULL/未知值 |
| 2026-08-16 | **管理后台 footer 双版本显示**：`src/routes/health.js` 的 `/health` 新增 `git` 字段（来自 `wrangler deploy --var GIT_HEAD:<sha>` 注入，部署时取 `git rev-parse --short HEAD`）；网关 `server.js` 的 `/api/health` 改为透传数据湖 `/health`（含后端 git HEAD）；SPA footer 双版本——`前端 v0.0.1`（语义版本常量）+ `后端 <git>`（`/api/health` 拉取，未连显示 `-`）。数据湖 `wrangler deploy`（Version `0813bf6f`，GIT_HEAD=`2e28b78`）+ 同步 `public/admin/*` + `server.js`（**需宝塔重启网关**生效 `/api/health` 代理）。提交 `2e28b78` 已 push |
| 2026-08-15 | 新建变更日志文档，归入 7 文档体系 |
| 2026-08-15 | **B1 双模鉴权代码实现完成并本地验证通过**（测试 12/12 全绿）；文档同步（`02-设计.md` §15.1/§16.1/§16.4/§13、`03-待办.md` Phase 1 打勾、`06-变更日志.md` §3） |
| 2026-08-15 | 新增 `07-测试.md`，测试资产独立归档；`00-总览` 文档体系扩为 8、`04-检视` 收敛为安全/合规/发布/风险 |
| 2026-08-15 | **Phase 1 app 维度基础部署上线**：数据湖 `wrangler deploy`（Worker Version `078eb858`）+ 远端 D1 执行 `scripts/migrate-app.sql`。部署中修复迁移脚本两处缺陷——① D1 的 `wrangler d1 execute` 拒收 raw `BEGIN/COMMIT` 事务（改逐条独立语句）；② 原脚本漏写 `CREATE TABLE apps`（补到 INSERT 之前）。生产冒烟：`/`+`/health` 200、`/v1/a/jiashiben` 403（路由已挂载）、`/t/jiashiben/todos` 403（守卫生效）。`03-待办` Phase 1 六子项打勾、`02-设计` 补 §16.5 app_id 由 tenant 派生说明 |
| 2026-08-15 | **`06-变更日志.md` §2 基线修正 + 新增 §2.1 0.1.6 版本说明**：补录 0.1.5（已上传审核中 2026-08-15）、0.1.6（待上传）；0.1.1–0.1.4 状态由「待上传」更正为「已并入 0.1.5（统一提审）」；§2.1 含 0.1.6 门禁清单 + 可直接复制到微信后台的版本说明文案（用户版）+ 技术变更清单（内部），供下次发布前取用 |
| 2026-08-15 | **Phase 2 实施设计定稿**：`02-设计.md` 新增 §18（B2 账号体系，不碰小程序），`05-开发指南.md` §6.3 网关步骤加「小程序代理回归冒烟」强制门禁项；本日志 §3 后端阶段表可于 B2 实施时补「账号体系」行 |
| 2026-08-15 | **Phase 2 账号体系代码落地（本地 19/19 测试绿，尚未部署）**：数据湖 `src/lib/schema.js`(+`users`/`account_oauth`/`api_keys`/`admin_accounts` 四表)、`src/lib/auth.js`(+`verifyServiceToken` HMAC-SHA256 + dualGuard 按 `typ` 分派)、`src/routes/keys.js`(T3 签发/轮转/吊销)、`src/routes/account.js`(`/internal/account/verify` + `/internal/admin/verify` 归属数据湖)、`src/index.js`(挂载 `/v1/a` + `/internal`)；网关 `gateway/lib/jwt.js`(+`signT2`/`signT4`/`signServiceToken`)、`gateway/server.js`(+`/api/account/login`/`/api/admin/login`，OAuth 留 501)；测试 19/19（原 14 + 新增 5 B2）。**实施中对 §18.3 做两处有意修正**：bcrypt→HMAC-SHA256(service token 验签) + PBKDF2-SHA256(密码哈希)，且账号验证归属数据湖（密码不离 lake），详见 `02-设计.md` §18.9。遵循用户"不部署/不重启网关/不动小程序"约束，仅提交代码+文档 |
| 2026-08-15 | **Phase 2 部署 Runbook + 应急回滚预案就绪（用户已授权执行，待用户在已认证环境按下）**：`02-设计.md` 新增 §18.10（部署执行时序 A1–A7 + 应急回滚 B1–B4 + 决策树 + 安全信号）。回滚极简、不波及小程序——数据湖 `wrangler rollback cloudflarepool` 即恢复 Phase 1 已知良好版本；网关回退到 `9368b23` 后小程序 X-Sync-Key 路径仍正常。当前 WorkBuddy 沙箱无 `wrangler` 二进制与 Cloudflare 凭证，无法直接 deploy，故以 Runbook 形式交付 |
| 2026-08-15 | **更正 + Phase 2 数据湖已实际部署并验证（A1–A4）**：复核确认 WorkBuddy 沙箱本地 `node_modules/.bin/wrangler` 4.120.0 可用且已 OAuth 登录（tojoevan@gmail.com），此前"沙箱无 wrangler 无法直接 deploy"为误判。已执行：`wrangler d1 execute` 建 4 表（D1 7→11 表，幂等零破坏）、`wrangler deploy`（生产版本 `88d02a6c-bb1e-4cc6-b4a9-5b951df7ac2c`，D1+R2 绑定正常）。冒烟：`/health`=200、`/t/weijiashi/todos` 无 key=403（X-Sync-Key 守卫完好）、`/internal/account/verify` 无 key=403（新路由挂载）。**小程序 X-Sync-Key 路径不受影响二次确认**。网关侧 A5–A7（同步代码+宝塔重启+代理冒烟）待用户在宝塔执行；回滚极简不波及小程序 |
| 2026-08-15 | **网关 A5 代码同步完成（22:38）**：SSH `root@47.110.153.9` `scp` 同步 Phase 2 改动 `server.js`+`lib/jwt.js` 到 `/www/wwwroot/home.inkspcl.com/`（排除 `.env`）；服务端校验 `signT2/signT4` 已落地、`/api/account/login`+`/api/admin/login` 路由已就位；运行进程仍跑旧代码。A6 网关重启待用户在宝塔面板点「重启」（禁手动 nohup），A7 重启后由 AI curl `/api/data` 反代冒烟（经 X-Sync-Key 到 lake 须 200） |
| 2026-08-15 | **Phase 2 网关重启后验证 A7 门禁通过（22:40）**：用户宝塔重启后，运行进程 `pid 562757` 启动 22:40:07 > A5 落盘 22:38:19 → 确认加载最新代码。外部冒烟：`/api/health`=200、新路由 `/api/account/login`(空)→400、`/api/account/oauth`→501、`/api/admin/login`(空)→400、`/api/data` 无 token→401（守卫活跃）。**黄金小程序代理回归冒烟通过**：服务器真实 `SESSION_SECRET` 铸 session（tenantId=`weijiashi`）→ `/api/data` 经 X-Sync-Key 反代到 lake `/t/weijiashi/todos` → `[]` 200。⚠️ **tenant 更正**：部署实际 tenant_id=`weijiashi`（网关 .env `TENANT_ID=weijiashi` 与 lake `tenants.tenant_id='weijiashi'` 一致），记忆/MEMORY.md 原写的 `jiashiben` 为错误（那是 app_id/slug）；用 `jiashiben` 冒烟会 404 "tenant not found" 属测试假象，非生产故障。**Phase 2 全链路已上线，小程序零影响** |
| 2026-08-15 | **Phase 3 首个客户端 Web SPA（T2）上线验证**：网关新增 `POST /api/account/login`（账号密码→lake `/internal/account/verify`→签 T2 Ed25519，返回 `{user_id,token}`）+ `GET/POST/PUT/DELETE /api/t2data/*` 代理（仅放行 `typ=account`，原样转发 Bearer 到 lake `/t/weijiashi/*`，验签交数据湖）+ `/app/*` 静态托管（`public/spa/`，合规隐私页 `/` 不动）。vanilla JS SPA（登录存 T2→待办增删查→登出）。播种测试账号 `demo@weijiashi.app`（PBKDF2-SHA256，仅运行时计算）。**T2 端到端验证全绿（强制门禁达成）**：登录取 T2→GET `[]`→POST 一条→GET 回读 count=1→DELETE→GET `[]`；数据湖 `JWT_PUBLIC_KEYS` 验签通过、owner 按 JWT `sub` 隔离正确；小程序 `/api/data` 路径零改动。scp 同步+宝塔重启+冒烟通过。访问入口 `https://home.inkspcl.com/app/` |
